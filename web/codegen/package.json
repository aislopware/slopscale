{
  "name": "headscale-admin-codegen",
  "private": true,
  "description": "Generates the API types from the server's OpenAPI document. Lives in its own workspace because openapi-typescript drives the TypeScript 5 compiler API, which the Go-based TypeScript 7 the app uses does not expose.",
  "type": "module",
  "scripts": {
    "generate": "openapi-typescript ../../gen/openapi/v1.yaml -o ../src/api/schema.gen.ts --root-types --root-types-no-schema-prefix --alphabetize --empty-objects-unknown"
  },
  "devDependencies": {
    "openapi-typescript": "7.13.0",
    "typescript": "5.9.3"
  }
}
