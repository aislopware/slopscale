node_modules/
# Build output; only the placeholder that keeps the go:embed pattern valid is tracked.
dist/*
!dist/.gitkeep
.vite/
*.tsbuildinfo
.vitest/
# Playwright e2e output
test-results/
playwright-report/
.tanstack/
# Tailscale's in-browser client, built by make wasm (cmd/tsconnect-build).
public/tsconnect/
